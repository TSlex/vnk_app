import { Context, Plugin } from "@nuxt/types";
import { Inject } from "@nuxt/types/app";

const ErrorHandler: Plugin = (ctx: Context) => {
  // ctx.$axios.onError(error => {
  //   return Promise.resolve(true)
  // })
}

export default ErrorHandler
